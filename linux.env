APP_MODE=config.ProdConfig
SECRET_KEY=EJFNhkafmndbsdrf7asdfkjlhf
APP_SERVER_OS=Linux

# Host folder used to persist SQLite DB + logs (bind-mounted to /data)
CDX_WEB_SCAN_HOST_DATA_DIR=/home/edbrad/CDX-Web-Scan

# Run the app container as this UID/GID so persisted files are host-owned (Linux)
CDX_WEB_SCAN_UID=1000
CDX_WEB_SCAN_GID=1000

# Optional: Intake API
# INTAKE_API_URL=
# INTAKE_API_TOKEN=