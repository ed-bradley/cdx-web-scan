# Docker/Compose environment variables (copy to .env and edit)

# Required
SECRET_KEY=change-me-to-a-long-random-string

# Optional: host folder to persist SQLite DB + logs (bind-mounted to /data in containers)
# Use an absolute path for "external folder" persistence, e.g. /home/you/Web-Scan-Data
CDX_WEB_SCAN_HOST_DATA_DIR=./cdx_data

# Intake API
INTAKE_API_URL=https://example.execute-api.us-east-1.amazonaws.com/prod/intake
INTAKE_API_TOKEN=
